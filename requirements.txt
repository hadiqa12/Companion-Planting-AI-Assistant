# Local embeddings + vector math (CPU-friendly; GPU optional if PyTorch sees a GPU)
sentence-transformers>=3.0.0
numpy>=1.24.0

# Loads variables from a local .env file into os.environ
python-dotenv>=1.0.0
